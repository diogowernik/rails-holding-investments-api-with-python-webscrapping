<div class="col-xl-12 mt-3">
    <% @expirations.each do |expiration| %>
        <div class="card border-success">
            <div class="card-title">
                <h4 class="text-success mt-4 ml-4">
                    <%= expiration.title %>
                </h4>
            </div>
            <div class="card-body">
                <table class="table table-striped table-bordered w-100" cellspacing="0">
                    <thead>
                        <tr>
                            <th>Status</th>
                            <th>Ticker</th>
                            <th>Resultado</th>
                            <th>Yield Sintético</th>
                            <th>Vencimento</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        <% @puts.where(:expiration_id => expiration.id).each do |put| %>
                        <tr>
                            <td><%= put.situation.title %></td>     
                            <td><%= put.put.ticker %></td>                         
                            <td class="text-primary"><%= number_with_precision(put.total_price, precision: 2, separator: ",", delimiter: ".") %></td>
                            <td class="text-primary"><%= number_with_precision((put.total_price/(put.shares_amount * put.put.strike))*100, precision: 2, separator: ",", delimiter: ".") %></td>
                            <td><%= put.put.expiration.title %></td>
                            <td><%= link_to 'E', edit_portfolio_put_path(put) %> | <%= link_to 'D', portfolio_put_path(put), method: :delete, data: { confirm: 'Are you sure?' } %></td>
                        </tr>
                        <% end %>
                    </tbody>
                </table>
            </div>
            <div class="card-footer">
                <h4 class="ml-2">
                Lucro total:
                <span class="text-primary">
                    <%= number_to_currency(@puts.where(:expiration_id => expiration.id).sum(:total_price), unit: "R$ ", separator: ",", delimiter: ".", strip_insignificant_zeros: true) %></h4>
                </span>
            </div>
        </div>
    <% end %>
    </div>