<div class="float-right"><%= link_to new_admin_cripto_path do %>
	<button class="btn btn-outline-success">
		<em class="fa fa-plus-circle"></em> &nbsp; Novo 
	</button><% end %>
</div>
<h3 class="h3-custom mb-4" id="minhas-paginas">
	<em class="fa fa-vcard-o text-success"></em> &nbsp; Criptomoedas
</h3>
<div class="table-responsive mt-2">
    <table id="" class="display table table-striped w-100" cellspacing="0">
      <thead>
        <tr>
          <th>ticker</th>
          <th>Preço</th>
          <th>title</th>
          <th>Url</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <% @criptos.each do |cripto| %>
          <tr>
            <td><%= cripto.ticker %></td>
            <td><%= cripto.price %></td>
            <td><%= cripto.title %></td>
            <td><%= cripto.slug %></td>
            <td> <%= link_to 'Edit', edit_admin_cripto_path(cripto.id) %> | <%= link_to 'Destroy', admin_cripto_path(cripto.id), method: :delete %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
</div>

