<%= simple_form_for([:admin, international_category]) do |form| %>
  <% if international_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(international_category.errors.count, "error") %> prohibited this international_category from being saved:</h2>

      <ul>
      <% international_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-6">
      <%= form.label :title %>
      <%= form.text_field :title, class: 'form-control' %>
    </div>
  <div class="col-md-6">
      <%= form.label :slug %>
      <%= form.text_field :slug, class: 'form-control' %>
    </div>

</div>
<div class="row">
    <div class="col-md-3 mt-4">
		<div class="form-group">
                <%= form.submit 'Enviar', class: 'btn btn-block btn-primary' %>
         </div>
     </div>
</div>
<% end %>
