<%= simple_form_for([:admin, br_stock]) do |form| %>
  <% if br_stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(br_stock.errors.count, "error") %> prohibited this br_stock from being saved:</h2>

      <ul>
      <% br_stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div class="row">
  <div class="col-md-3">
      <%= form.label :title %>
      <%= form.text_field :title, class: 'form-control' %>
    </div>
  <div class="col-md-3">
      <%= form.label :slug %>
      <%= form.text_field :slug, class: 'form-control' %>
  </div>
    <div class="col-md-3">
      <%= form.label :ticker %>
      <%= form.text_field :ticker, class: 'form-control' %>
    </div>
  <div class="col-md-3">
      <%= form.label :price %>
      <%= form.text_field :price, class: 'form-control' %>
  </div>

</div>
<div class="row">
    <div class="col-md-3 mt-4">
		<div class="form-group">
                <%= form.submit 'Enviar', class: 'btn btn-block btn-primary' %>
         </div>
     </div>
</div>
<% end %>
